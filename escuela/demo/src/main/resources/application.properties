spring.application.name=ProyectoPrueba
server.port=${PORT:8080}

# Conexión a PostgreSQL (Render)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d6j2o4ea2pns7393hpcg-a.virginia-postgres.render.com:5432/escuela_8axw?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:escuela_8axw_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:OiV2qwUmewzWkpVAtFzksUz5aMTdNqXa}
spring.datasource.driver-class-name=org.postgresql.Driver

# Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Seeding & Safety
app.seeding.enabled=${APP_SEEDING_ENABLED:false}
app.db.cleaner.enabled=${APP_DB_CLEANER_ENABLED:false}